# All settings have to start with REACT_APP_ inorder for the create-react-app build script to pick them up
# and make them available on process.env
NODE_OPTIONS=--openssl-legacy-provider
SKIP_PREFLIGHT_CHECK=true
